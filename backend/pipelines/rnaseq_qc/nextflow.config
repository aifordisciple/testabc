docker {
    enabled = true
    // 强制使用 linux/amd64 平台 (因为生物信息镜像大多是 x86 的)，并使用当前用户权限
    runOptions = '-u $(id -u):$(id -g) --platform linux/amd64'
}

process {
    // 默认兜底镜像
    container = 'ubuntu:20.04'
    errorStrategy = 'finish'
}